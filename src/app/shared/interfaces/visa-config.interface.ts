export interface IVisaConfig {
  action: string;
  channel?: string;
  token: string;
  merchantId: number;
  buttonText: string;
  purchaseNumber: number;
  ammount: string;
  name: string;
  lastName: string;
  email: string;
}
